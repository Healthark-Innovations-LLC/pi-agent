FROM pi-agent:latest

RUN apt update && \
    apt install -y build-essential python3-dev

RUN curl -fsSL https://bootstrap.pypa.io/get-pip.py | python3 - --break-system-packages && \
    python3 -m pip install -U pip --break-system-packages

# python packages automonisaur
RUN python3 -m pip install -U -r https://raw.githubusercontent.com/TheShellLand/automonisaur/refs/heads/master/requirements.txt

# ansible
RUN git clone https://github.com/TheShellLand/antsable /antsable && \
    cd /antsable && \
    bash install-ansible.sh && \
    # chrome, docker
    bash ansible-local.sh playbooks/readyup.yml

